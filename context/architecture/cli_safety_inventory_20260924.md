# Инвентаризация CLI по A05 — 24.09.2026

Охват: все 32 PHP-файла в `moodle/local/ustar/cli/` на текущей ветке PR #14. Это инженерная классификация исходников, не подтверждение исполнения на живой базе. Исторические копии сохранены в `context/archive/legacy_cli/`. До релиза нужно проверить фактическую конфигурацию заданий сервера, право запуска команд и результаты restore rehearsal.

| Категория | Команды | Текущее решение |
|---|---|---|
| Выведенные одноразовые и опасные (10) | `hr_canonical_2724_apply`, `probe_development_assessment`, `ensure_demo_executive`, `normalize_demo_positions`, `ensure_tasks_2725`, `assessment_lifecycle_probe`, `reconcile_assessment_runtime`, `import_structure`, `sync_position_access`, `import_reporting` | Старые рабочие пути завершаются кодом 78 до подключения Moodle; исходники сохранены для аудита. Массовый repair аттестаций требует нового scoped-инструмента, если он понадобится. |
| Разрешённая запись с явным планом (3) | `import_catalog`, `import_catalog_manifest`, `adjust_uscoin` | По умолчанию отчёт без записи; `--apply`, контрольная сумма входа/плана и ссылка на backup обязательны. Оператор обязан проверить реальное наличие backup: параметр сам этого не доказывает. Для бинарного импорта хеш включает вложения. |
| Изолированные фикстуры (2) | `materials_library_ui_fixture`, `test_materials_library` | Исходники требуют локальный адрес стенда и `noemailever`; выполнение на производственной конфигурации не допускается. Проверить ложные срабатывания после запуска на стенде. |
| Планируемые операции, ещё не принятые по A05 (3) | `bootstrap_trading_floor_route`, `migrate_stage2_access`, `sync_staff_map` | Имеют форму preview/apply или флаг применения, но backup reference, точный target manifest и влияние на канонический оргдомен ещё требуют ревью. Не запускать на prod до отдельной проверки. |
| Создание резервной копии (1) | `archive_courses` | Создаёт Moodle backups и файлы архива; это операционная запись в хранилище, не удаление исходных курсов. Проверить объём, права на файлы и восстановление `.mbz`. |
| Чтение или отчёт по исходнику (13) | `audit_access`, `audit_domain_integrity`, `check_catalog_archobuch`, `check_development_assessment`, `check_learning_route_v2`, `check_learning_route_v2_schema`, `check_materials_library_schema`, `check_role_ui`, `check_route_user`, `reconcile_organization`, `sync_uscoin`, `validate_2725`, `validate_2726` | Нет явных записей через вызовы `$DB` в этих CLI. Проверить побочные эффекты вызываемых сервисов; исторические `validate_2725/2726` сравнивают старые версии и не являются релизным барьером. |

Оставшийся риск A05: наличие переключателя `--apply` недостаточно для безопасной массовой операции; необходимо ограничить область, привязать план к точной версии входа и сверить результат на восстановленном стенде. Хранение исторических файлов в репозитории не означает разрешения их запуска.

Дополнение к PR №14: `check_route_user` больше не вызывает `route_model::for_user`, который зачисляет в Moodle и пересчитывает прогресс. Команда использует `read_only_snapshot`, по умолчанию выбирает фактическую должность человека и явно помечает вывод как сохранённый снимок. Полнота списка остальных диагностических команд и поведение на восстановленной базе ещё требуют проверки; статус A05 не меняется автоматически.
