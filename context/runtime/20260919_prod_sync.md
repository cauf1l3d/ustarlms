# Production source reconciliation — 2026-09-19

## Exact source baseline

Branch:

`integration/ustar-20260919`

Commit:

`378d397152a8c83f8b0d046e2e561ab2732d6b02`

Previous source baseline:

`integration/ustar-20260912@48326c6a011f58b985d251cb0462835bc1d37a16`

## Runtime evidence

Observed by the user-executed reconciliation script on 2026-09-19:

- `ustar_moodle` — image `stack-moodle`, `127.0.0.1:8082->80/tcp`
- `ustar_postgres` — image `postgres:16`
- host Moodle mount: `/opt/ustar/data/moodle/public -> /var/www/html`
- host moodledata mount: `/opt/ustar/data/moodle/moodledata -> /var/www/moodledata`

Runtime reference timestamp recorded in the source commit:

`2026-09-19T14:43:52+00:00`

## Source reconciliation result

Production roots:

- `/opt/ustar/data/moodle/public/public/local/ustar`
- `/opt/ustar/data/moodle/public/public/theme/ustar`

Git roots:

- `moodle/local/ustar`
- `moodle/theme/ustar`

Verification:

`INDEX_SOURCE_BYTE_MATCH_OK`

Publishable Git source:

- local/ustar: 328 files
- theme/ustar: 53 files
- total: 381 files

Each Git-indexed source blob was checked byte-for-byte against the production-derived source before commit.

Production directory inventory after the explicit cleanup pass:

- total files: 414
- Git publishable: 381
- ignored developer/runtime backup artifacts: 33

The ignored artifacts are recorded in the production-sync branch and are not treated as active application source.

## Recovery snapshot

Full recovery archive:

`USTAR_FULL_CURRENT_20260919_113924.tar.gz`

SHA256:

`598177f211333d7d036abc025b9d21c3c7c3eb4cfb0bc10d004ff0fcff473f41`

The recovery archive contains runtime data that Git intentionally does not contain, including PostgreSQL, moodledata and exact Docker images. It is not committed to GitHub.

## What this evidence proves

It proves that the publishable application source in the exact Git commit matches the production-derived source used for reconciliation.

It does not independently prove:

- full database schema correctness;
- every Moodle upgrade path;
- browser acceptance of every screen;
- native tour records in DB;
- successful restore of the recovery archive onto a separate clean host.

Those remain R00 acceptance work.
